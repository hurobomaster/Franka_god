# Franka ACT 控制端部署交接说明

## 1. 本次使用的模型路径（固定）

- `CKPT_PATH=/home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/last/pretrained_model`

## 2. 目标

- 在控制端机器加载上述 ACT checkpoint。
- 先做离线加载冒烟。
- 再做真机低风险短时试跑。
- 最后做评估录制（用于复盘）。

## 3. 安全前置（必须先执行）

1. 确认急停可用，现场有人值守。
2. 首次试跑清空工作空间障碍物，机械臂速度和加速度设为保守档。
3. 先空抓（无物体）试跑 1-2 次，再进入插入任务。
4. 任何异常抖动、冲撞趋势，立即急停并退出策略控制。

## 4. 环境准备

```bash
cd /home/asus/hu_rz/teleop/lerobot
source /home/hurz/miniconda3/bin/activate lero
uv --version
python --version
```

## 5. checkpoint 完整性检查

```bash
CKPT_PATH=/home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/last/pretrained_model

test -f ${CKPT_PATH}/config.json && echo "[ok] config.json"
test -f ${CKPT_PATH}/model.safetensors && echo "[ok] model.safetensors"
ls -lah ${CKPT_PATH}
```

## 6. 策略加载冒烟（不连真机）

```bash
CKPT_PATH=/home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/last/pretrained_model

uv run python - <<'PY'
import torch
from lerobot.policies.act.modeling_act import ACTPolicy

ckpt = "/home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/last/pretrained_model"
policy = ACTPolicy.from_pretrained(ckpt)
policy.eval().to("cuda:0")

batch = {
    "observation.state": torch.zeros(1, 8, device="cuda:0"),
    "observation.images.front": torch.zeros(1, 3, 480, 640, device="cuda:0"),
    "observation.images.wrist": torch.zeros(1, 3, 480, 640, device="cuda:0"),
}
with torch.no_grad():
    out = policy.predict_action_chunk(batch)
print("predict_action_chunk shape =", tuple(out.shape))
PY
```

预期：

- 正常打印形状，通常为 `(1, chunk_size, 8)`。
- 不应出现模型加载失败或维度不匹配。

## 7. 真机短时试跑（推荐先用 lerobot-rollout）

说明：

- 下面命令不写死 `robot.type`，请按控制端真实配置填写。
- 首次建议 `duration=20~30` 秒，`task` 用最简单描述。

```bash
export CUDA_VISIBLE_DEVICES=0

CKPT_PATH=/home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/last/pretrained_model
ROBOT_TYPE=<填写控制端机器人类型>
ROBOT_PORT=<例如 /dev/ttyACM0>
ROBOT_CAMERAS='<按控制端实际相机配置填写>'
TASK_TEXT="grasp and insert"
FPS=10

lerobot-rollout \
  --strategy.type=base \
  --policy.path=${CKPT_PATH} \
  --robot.type=${ROBOT_TYPE} \
  --robot.port=${ROBOT_PORT} \
  --robot.cameras="${ROBOT_CAMERAS}" \
  --task="${TASK_TEXT}" \
  --fps=${FPS} \
  --duration=30 \
  --device=cuda \
  --display_data=true
```

## 8. 真机评估录制（推荐）

说明：

- 使用 `lerobot-record` 执行策略并录成评估集。
- 评估集建议前缀 `eval_`。

```bash
export CUDA_VISIBLE_DEVICES=0

CKPT_PATH=/home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/last/pretrained_model
ROBOT_TYPE=<填写控制端机器人类型>
ROBOT_PORT=<例如 /dev/ttyACM0>
ROBOT_ID=<自定义机器人ID>
ROBOT_CAMERAS='<按控制端实际相机配置填写>'
TASK_TEXT="grasp and insert"
EVAL_REPO_ID=<例如 local/eval_franka_insert_act>

lerobot-record \
  --robot.type=${ROBOT_TYPE} \
  --robot.port=${ROBOT_PORT} \
  --robot.id=${ROBOT_ID} \
  --robot.cameras="${ROBOT_CAMERAS}" \
  --display_data=true \
  --dataset.repo_id=${EVAL_REPO_ID} \
  --dataset.num_episodes=10 \
  --dataset.single_task="${TASK_TEXT}" \
  --dataset.streaming_encoding=true \
  --dataset.encoder_threads=2 \
  --policy.path=${CKPT_PATH}
```

## 9. 异常处理与回退

1. 若出现维度错误：
   - 核对 `observation.state` 是否 8 维。
   - 核对 `front/wrist` 两路图像分辨率是否与训练一致（640x480）。
2. 若出现控制抖动：
   - 先降低 `FPS`（例如 `10 -> 5`）。
   - 缩短 `duration`，改为空抓测试。
3. 若出现设备占用或串口异常：
   - 断开并重连机器人通信，再重启 rollout/record 进程。
4. 无法稳定执行时，立即回退到 teleop 人工控制，停止策略控制。

## 10. 交付回报格式（控制端 agent 执行后返回）

- 实际执行命令（完整）。
- 冒烟结果（是否成功、输出形状）。
- 短时试跑结果（是否平稳、是否触发急停）。
- 评估数据位置（dataset repo_id 或本地路径）。
- 问题清单与下一步建议。
