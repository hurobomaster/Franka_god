# Franka ACT 笔记本 4070 部署交接

## 1. 背景信息与角色定义
- 训练端: 服务器，只负责训练与导出模型。
- 部署端: 4070 笔记本，负责实时相机采集、策略推理、下发机器人动作。
- 控制端: 无显卡台式，当前部署链路中不参与实时推理闭环，仅作为备用。

当前默认采用部署端单机闭环，不做控制端转发。


conda环境用 hu_franka


## 2. 当前网络与通信状态

### 2.1 关键信息
- 控制端 IP: 10.19.131.200
- Franka IP: 10.19.131.202
- Franka 自带夹爪 IP: 192.168.1.114
- 笔记本有线网卡 IP: 10.19.134.16/21

### 2.2 实测通信
- 笔记本 ping 控制端: 通，0% 丢包，约 0.1 到 0.2 ms。
- 笔记本 ping Franka: 通，0% 丢包，约 0.1 到 0.3 ms。
- 路由: 10.19.131.202 走有线网卡 enp8s0。

结论: 当前交换机网络可用于部署端直接连接 Franka。

## 3. 仓库与关键路径
- teleop 根目录: /home/asus/hu_rz/teleop
- lerobot 根目录: /home/asus/hu_rz/lerobot
- checkpoint 路径: /home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/060000/pretrained_model
- home pose 文件: /home/asus/hu_rz/teleop/info/franka_home_joints.json
- 环境导出文件: /home/asus/hu_rz/teleop/env_franka.yml

## 4. Franka 端需要的数据顺序与格式

### 4.1 模型输入 observation（部署端采集后构造）
1. observation.state
- 类型: float32
- 形状: 8
- 顺序:
  - 0: joint_1
  - 1: joint_2
  - 2: joint_3
  - 3: joint_4
  - 4: joint_5
  - 5: joint_6
  - 6: joint_7
  - 7: gripper_width（米）

2. observation.images.front
- 类型: 原始采集为 HWC，部署前转为 CHW
- 推理张量类型: float32
- 推理形状: 1 x 3 x 480 x 640

3. observation.images.wrist
- 类型与前置相机一致
- 推理形状: 1 x 3 x 480 x 640

### 4.2 模型输出 action（发送给 Franka）
- 类型: float32
- 形状: 8
- 顺序:
  - 0 到 6: joint_vel_1 到 joint_vel_7（关节速度命令）
  - 7: gripper_target_width（夹爪目标宽度，米）

注意: 第 8 维不是开关信号，而是宽度值。

## 5. 当前部署逻辑

### 5.1 非 dry-run 行为
1. 加载 policy。
2. 创建并连接 FrankaRobot。
3. 自动复位:
- 优先读取 /home/asus/hu_rz/teleop/info/franka_home_joints.json。
- move_to_home。
- 等待 3 秒。
4. 进入循环:
- 读机器人状态和双相机。
- 构造 observation。
- policy.predict_action_chunk。
- 取第 1 步 action 下发。

### 5.2 dry-run 行为
- 只做模型加载与一次前向，不连接机器人和相机。

## 6. 环境核对与恢复

### 6.1 GPU 环境核对
```bash
nvidia-smi
python -c "import torch; print(torch.__version__, torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'no-gpu')"
```

预期:
- 能识别 4070。
- cuda 可用。

### 6.2 环境恢复
```bash
conda env create -n hu_franka -f /path/to/env_franka.yml
conda activate hu_franka
```

至少保证以下模块可导入:
- torch, torchvision
- safetensors
- pyrealsense2
- franky
- lerobot

## 7. 测试流程

### 7.1 第一步: 离线 dry-run
```bash
cd /home/asus/hu_rz/teleop
/home/asus/miniconda3/envs/hu_franka/bin/python deploy_policy.py \
  --policy-path /home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/060000/pretrained_model \
  --dry-run \
  --device cuda
```

预期:
- Loaded successfully on cuda
- Policy output shape: (1, 100, 8)

### 7.2 第二步: 真机短测（先低风险）
```bash
cd /home/asus/hu_rz/teleop
/home/asus/miniconda3/envs/hu_franka/bin/python deploy_policy.py \
  --policy-path /home/asus/hu_rz/teleop/model/act_franka_insert_baseline/checkpoints/060000/pretrained_model \
  --device cuda \
  --fps 5 \
  --duration 10 \
  --front-camera-id 327522300259 \
  --wrist-camera-id 153222071977 \
  --robot-ip 10.19.131.202
```

如需先屏蔽夹爪:
```bash
--disable-gripper
```

### 7.3 第三步: 增量放大
通过后再逐步增加:
- duration: 10 -> 30 -> 60
- fps: 5 -> 8 -> 10

## 8. 常见问题
1. 相机识别名不一致
- 使用序列号，不用设备名字符串。

2. 进程 core dumped
- 先加 --disable-gripper。
- 降低 fps 和 duration 做定位。

3. CUDA 不可用
- 检查 torch CUDA 版本和驱动。

## 9. 关键代码改动（必须同步）
- deploy_policy.py
- lerobot/src/lerobot/robots/franka/config_franka.py
- lerobot/src/lerobot/robots/franka/franka_robot.py
- lerobot/src/lerobot/robots/utils.py
- lerobot/src/lerobot/policies/pretrained.py
- lerobot/src/lerobot/policies/factory.py
- lerobot/src/lerobot/policies/pi0/modeling_pi0.py
- lerobot/src/lerobot/policies/pi05/modeling_pi05.py
- lerobot/src/lerobot/policies/pi0_fast/modeling_pi0_fast.py
- lerobot/src/lerobot/policies/smolvla/modeling_smolvla.py

## 10. 提交与同步
- 已在 teleop/.gitignore 忽略 safetensors 大文件。
- 部署命令使用固定 checkpoint 路径，不使用 last。
