# 部署参数优化与实时调度改动总结

## 🎯 改动目标
- 从 CPU 限制的 3Hz 升级到 GPU 支持的 5Hz（匹配训练频率）
- 启用真实时调度（Enforce）替代非实时降级方案

## ✅ 已完成改动

### 1️⃣ deploy_policy.py 频率对齐

| 参数 | 之前 | 现在 | 说明 |
|------|-----|------|------|
| --fps (默认) | 30.0 Hz | **5.0 Hz** | 匹配训练条件 |
| --duration (默认) | 60.0 s | **10.0 s** | 50步 @5Hz = 10秒 |
| 前摄像头 fps | 30 Hz | **5 Hz** | 同步控制循环 |
| 腕摄像头 fps | 30 Hz | **5 Hz** | 同步控制循环 |

**效果**：
```
之前：50步用时 21.7秒 (实际 2.3Hz) ❌
现在：50步应用时 ~10秒 (5Hz) ✅
```

### 2️⃣ franka_robot.py 实时调度改进

**之前的逻辑**（保守模式）：
```
FRANKA_REALTIME_ENFORCE=1
  ├─ 尝试 RealtimeConfig.Enforce
  └─ 失败 → 回退到 RealtimeConfig.Ignore

否则：
  └─ 直接使用 RealtimeConfig.Ignore（非实时）
```

**现在的逻辑**（积极模式）：
```
FRANKA_REALTIME_DISABLE≠1
  ├─ 尝试 RealtimeConfig.Enforce（默认）✨
  └─ 失败 → 回退到 RealtimeConfig.Ignore（自动降级）

FRANKA_REALTIME_DISABLE=1
  └─ 直接使用 RealtimeConfig.Ignore（显式禁用）
```

**优点**：
- ✅ GPU 笔记本会自动获得实时调度
- ✅ 权限不足时优雅降级，不影响部署
- ✅ 环境变量语义更清晰（DISABLE 比 ENFORCE 更直观）

## 🚀 快速使用

### 基础部署（新频率）
```bash
cd /home/rune/proj_rune/franka_algorithm/Franka_god
python deploy_policy.py \
    --policy-path model/act_franka_insert_baseline/checkpoints/080000/pretrained_model \
    --robot-ip 10.19.131.202 \
    --duration 10  # 自动 5Hz（默认）
```

### 如果遇到实时权限问题
系统会自动降级，但可按 `实时调度配置指南.md` 配置权限以获得最佳性能。

### 手动禁用实时调度
```bash
FRANKA_REALTIME_DISABLE=1 python deploy_policy.py ...
```

## 📊 预期改进

### 控制频率
| 条件 | 之前 | 现在 | 改进 |
|------|-----|------|------|
| CPU 电脑 | 3.0 Hz (极限) | 5.0 Hz (GPU支持) | +67% ⬆️ |
| 50步耗时 | ~17-22秒 | ~10秒 | **-50% ⏱️** |
| 任务成功率 | 低（频率不匹配） | 高（分布一致） | 显著提升 ✨ |

### 实时性
- **前**：依赖环境变量和 try-except 回退
- **后**：优先获取实时调度，权限问题自动处理
- **效果**：控制循环延迟更均匀，P95/P99 更低

## 📝 技术细节

### 训练配置确认
```json
{
  "policy": {
    "n_obs_steps": 1,        // 观测步数
    "chunk_size": 100,       // 动作块大小
    "n_action_steps": 100,   // 预测步数
    "optimizer_lr": 1e-4
  },
  "steps": 100000,           // 总训练步
  "fps": 5                   // 训练采集频率
}
```

### 状态/动作维度
- 状态：8D (7 关节位置 + 1 夹爪宽度)
- 动作：8D (7 关节速度 + 1 夹爪目标宽度)
- 图像：两路 (前摄 + 腕摄) 各 640×480 RGB

## 🔧 如需回滚
不推荐回滚，但如需要：

```bash
# 恢复到之前的 CPU 模式
FRANKA_REALTIME_DISABLE=1 python deploy_policy.py \
    --fps 3 \
    --duration 60 \
    ...
```

## 📚 参考文档
- 详细实时调度配置：见 `实时调度配置指南.md`
- 训练配置：`model/act_franka_insert_baseline/checkpoints/080000/pretrained_model/train_config.json`
- 部署脚本：`deploy_policy.py`
